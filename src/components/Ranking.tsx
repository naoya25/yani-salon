import React from "react";
import Post from "../types/Post";

const Ranking: React.FC<{ posts: Post[] }> = ({ posts }) => {
  return (
    <div>
      <h3>Ranking</h3>
    </div>
  );
};

export default Ranking;
